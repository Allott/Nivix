const alphabet = require('./fireMind.js');


var myArgs = process.argv;

if (myArgs.length === 2) {
    console.log("no input");
}
else
{
    var s = "";
    for (var i = 2; i < myArgs.length; i++) {
        s += " " + myArgs[i];
    }
    console.log(s);
}
